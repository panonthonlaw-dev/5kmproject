import streamlit as st
from streamlit_gsheets import GSheetsConnection

# --- ฟังก์ชันเช็ค Password (ใช้ชุดเดิมจากครั้งที่แล้ว) ---
def check_password():
    if not st.session_state.get("authenticated", False):
        with st.form("login"):
            st.subheader("🔐 เข้าสู่ระบบ")
            user = st.text_input("Username")
            pw = st.text_input("Password", type="password")
            if st.form_submit_button("Login"):
                if user in st.secrets["users"] and pw == st.secrets["users"][user]:
                    st.session_state["authenticated"] = True
                    st.rerun()
                else:
                    st.error("❌ ข้อมูลไม่ถูกต้อง")
        return False
    return True

if check_password():
    st.title("📊 รายงานข้อมูลเฉพาะคอลัมน์")

    try:
        conn = st.connection("gsheets", type=GSheetsConnection)
        df = conn.read(ttl="1m")

        # --- ส่วนการเลือกเฉพาะคอลัมน์ A, AL, AM, AN ---
        # วิธีที่ 1: เลือกด้วยตำแหน่ง (Index)
        # ใช้ .iloc[:, [ตำแหน่งคอลัมน์]] 
        df_selected = df.iloc[:, [0, 37, 38, 39]]

        # แสดงผลตารางที่กรองคอลัมน์แล้ว
        st.subheader("ข้อมูลคอลัมน์ A, AL, AM, AN")
        st.dataframe(df_selected, use_container_width=True)

        # วิธีที่ 2: (แถม) ถ้าต้องการระบุด้วยชื่อหัวคอลัมน์ตรงๆ 
        # ให้ใช้โค้ดแบบนี้แทน (เปลี่ยนชื่อให้ตรงกับหัวตารางของคุณ):
        # df_selected = df[["ชื่อหัวคอลัมน์A", "ชื่อหัวคอลัมน์AL", "ชื่อหัวคอลัมน์AM", "ชื่อหัวคอลัมน์AN"]]

    except Exception as e:
        st.error(f"ไม่สามารถแสดงข้อมูลได้: {e}")
        st.info("คำแนะนำ: ตรวจสอบว่า Google Sheet ของคุณมีจำนวนคอลัมน์ถึง AN (คอลัมน์ที่ 40) หรือไม่")
